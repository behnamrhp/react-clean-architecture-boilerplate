export class GetUsersInfoDTO {
  /* -------------------------------------------------------------------------- */
  public id: string;

  /* -------------------------------------------------------------------------- */
  public name: string;

  /* -------------------------------------------------------------------------- */
  public email: string;

  /* -------------------------------------------------------------------------- */
  constructor({
    id,
    name,
    email,
  }: {
    id: string;
    name: string;
    email: string;
  }) {
    this.id = id;
    this.email = email;
    this.name = name;
  }
  /* -------------------------------------------------------------------------- */
}
