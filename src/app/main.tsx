import 'reflect-metadata';
import React from "react"
import ReactDOM from "react-dom/client"
import App from "./support/initialize/App"

ReactDOM.createRoot(document.getElementById("root")!).render(<App />)
