export default class BaseUrlAPIs {
  /* -------------------------------------------------------------------------- */
  authenticationURL = ENV.AUTH_URL;

  /* -------------------------------------------------------------------------- */
  mainURL = ENV.MAIN_URL;

  /* -------------------------------------------------------------------------- */
  connectionTimeout = 4000;

  /* -------------------------------------------------------------------------- */
  receiveTimeout = 7000;

  /* -------------------------------------------------------------------------- */
  sendTimeout = 4000;

  /* -------------------------------------------------------------------------- */
}
